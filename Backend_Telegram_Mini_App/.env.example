# Copy to .env for local testing (the worker reads these variables).
# MINI_BOT_SERVICE_KEY is OPTIONAL - the built-in default matches the main API.
MINI_BACKEND_BASE_URL=http://localhost:8000
# MINI_BOT_SERVICE_KEY=set-your-own-secret-here
LOOP_SECONDS=0.5
