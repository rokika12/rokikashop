# Copy to .env.local and edit
# Backend API base. Leave UNSET on Netlify (the /api proxy is used).
# Local development:
REACT_APP_API_URL=http://localhost:8000

# Where images live (banners, logos, products, QR). Defaults to the deployed backend.
# REACT_APP_MEDIA_URL=http://localhost:8000
